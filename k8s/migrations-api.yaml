apiVersion: apps/v1
kind: Deployment
metadata:
  name: migrations-api
  namespace: nutri-veda
spec:
  replicas: 1
  selector:
    matchLabels:
      app: migrations-api
  template:
    metadata:
      labels:
        app: migrations-api
    spec:
      automountServiceAccountToken: false
      containers:
      - name: migrations-api
        image: migrations-api:1.0.0
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_URLS
          value: http://0.0.0.0:8080
        - name: ConnectionStrings__Default
          valueFrom:
            secretKeyRef:
              name: db-conn
              key: ConnectionStrings__Default
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
            ephemeral-storage: "256Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: migrations-api
  namespace: nutri-veda
spec:
  selector:
    app: migrations-api
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
